import { getPopover } from "../../../surveyUtils.js";
import { progressBarColor } from "../surveyBar.js";

export function initSurveyAllgergiInfo() {
    getPopover()
    progressBarColor("allergiinfo")
}